define([
	"jquery",
	"use!backbone",
	"use!underscore",
	"use!ui",
	"_s",
	"TableSelect",
		],
function($, Backbone, _, ui, _s, TableSelect){

	var SASIFileTableSelectView = TableSelect.views.TableSelectView.extend({
	});

	return SASIFileTableSelectView;
});
		

